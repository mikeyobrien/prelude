;;; evil-config.el --- Early Evil configuration
;;
;; Set evil-want-keybinding to nil before Evil loads
;; This is required for evil-collection to work properly

(setq evil-want-keybinding nil)
(setq evil-want-integration t) ; This is optional but recommended

(provide 'evil-config)
;;; evil-config.el ends here